[phases.setup]
nixPkgs = ["nodejs_22", "gcc", "gnumake", "python3"]

[phases.install]
cmds = ["npm ci --prefer-offline --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/index.js"

[variables]
NPM_CONFIG_CACHE = "/tmp/npm-cache"
